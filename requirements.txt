# JZL MiniMax H3 节点依赖
# =======================

# 本地 LLM/VLM 推理：不再需要 llama-cpp-python！
# 改用 install_runtime.py 部署的 llama-server 子进程（llama.cpp b10436 预编译），
# 跨平台自动识别显卡 + SHA256 校验，零 pip 依赖。
# 首次使用前请在节点目录执行：python install_runtime.py

# imageio-ffmpeg（漫剧资产管理节点「视频」抽帧兜底）
# 系统已装 ffmpeg 可跳过；没有 ffmpeg 时视频加载会失败，装上即可：
imageio-ffmpeg
